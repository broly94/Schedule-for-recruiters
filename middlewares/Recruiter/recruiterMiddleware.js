import recruiterSchema from "../../models/recuiterModel.js";
import bcrypt from 'bcrypt';

const verifyDataRepeatUpdate = async (req, res, next) => {
    try {
        
        

    } catch (error) {
        
    }
}

export {
    verifyDataRepeatUpdate
}